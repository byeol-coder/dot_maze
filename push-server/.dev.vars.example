# Copy to `.dev.vars` for local `wrangler dev` (this file is gitignored).
# In production set the private key as a secret instead:
#   wrangler secret put VAPID_PRIVATE
VAPID_PRIVATE = "your-vapid-private-key-here"
